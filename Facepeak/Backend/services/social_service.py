"""
Social features API (stub).

This endpoint intentionally exposes feature availability only.
All social functionality (sharing, friends, chat, looksmatch) is
disabled in the current version and will be enabled incrementally
behind feature flags in future releases.

This file defines the API contract early to keep frontend and backend
in sync without introducing premature logic or dependencies.
"""